/// Label Flutter web gives the placeholder button that turns on its semantics
/// tree.
///
/// The browser clicks it to expose the tree, and the extractor drops it as
/// engine chrome rather than app content.
const String flutterAccessibilityToggleLabel = 'Enable accessibility';
